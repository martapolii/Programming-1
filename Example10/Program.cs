namespace Example10
    {
        class Program
        {
            static void Main(string[] args)
            {
              /* WEEK 5
               * 
               * CONTROL STRUCTURES - allow you to establish more control over what a program should do in certain situations */


            // IF STATEMENTS:
            // A conditional structure that determines whether a specified code block will run.
            // (a conditional structure is a type of control structure)

            Console.WriteLine("Line 14 runs.");

                if (true)
                {
                    // This code will run if condition = true.
                    Console.WriteLine("Line 19 runs.");
                }

                Console.WriteLine("Line 16 runs.");

                if (3 > 5)
                { // false
                    Console.WriteLine("Line 26 runs.");
                //runs if and only if the condition returns true
                }
            }
        }
    }
}
    }
}
